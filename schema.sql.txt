CREATE TABLE employees (
    employee_id INT PRIMARY KEY,
    name VARCHAR(50),
    department VARCHAR(50),
    performance_score INT,
    is_active BOOLEAN,
    join_date DATE,
    leave_date DATE
);
